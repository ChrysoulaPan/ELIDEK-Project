<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
</head>

<body>
    <div class="container">
        <div class="row" id="row">
            <div class="col-md-12">
                <div class="card" id="card-container">
                    <div class="card-body" id="card">
                    <% if (young_res.length == 0) { %> 
                        <h1 style="margin-top: 5rem;">No researchers found!</h1>
                    <% } else { %>
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Name</th>
                                        <th>Surname</th>
                                        <th>Project Count</th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% young_res.forEach((young_res, _) => { %>
                                        <tr>
                                            <td><%= young_res.researcher_id %></td>
                                            <td><%= young_res.researcher_name %></td>
                                            <td><%= young_res.researcher_surname %></td>
                                            <td><%= young_res.proj_count %></td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
